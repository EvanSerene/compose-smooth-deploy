# deploy.sh 微服务零停机平滑发布脚本使用说明

单机 Docker Compose 场景下的发布工具：构建镜像、平滑滚动发布、失败自动回滚、手动回滚一体化，全部通过命令行完成。

## 一、功能特性

- 平滑滚动发布：逐副本替换，全程零停机（探活通过才停旧容器）
- 应用级健康检测：通过 actuator/health 接口探活，未就绪不发流量
- 失败自动回滚：任意环节异常自动清理新容器、恢复旧版本
- 手动回滚：支持回滚到上一版本或指定 tag
- 发布历史记录：每次发布自动写入日志文件，作为回滚依据
- 并发锁：flock 按服务粒度加锁，防止同一服务并发发布/回滚
- 双模式发布：白名单服务走平滑滚动，其余服务走停机发布

## 二、环境要求

| 依赖 | 要求 |
|---|---|
| 操作系统 | Linux（脚本为 bash） |
| Docker Engine | 任意较新版本 |
| docker compose | **v2.x（推荐）**；v1.25+（`docker-compose`）理论兼容但已停止维护 |
| 其他命令 | curl（健康检测）、flock（并发锁，util-linux 自带）、sed、awk |

## 三、目录与文件约定

| 路径 | 说明 |
|---|---|
| deploy.sh | 本脚本，可放任意位置，建议与 compose 文件同目录 |
| docker-compose.yml | Compose 配置文件（`COMPOSE_DIR` 指向的目录） |
| release_history.log | 发布历史，首次发布自动生成，勿手动编辑 |
| /tmp/deploy-<服务名>.lock | 并发锁文件，自动生成，**勿手动删除**（见并发锁章节） |

## 四、配置区说明（脚本顶部，发布前按需修改）

脚本顶部 4 个配置块，新增/调整服务全部在这里完成：

1. 固定路径配置：源码根目录 `APP_DIR`、compose 目录 `COMPOSE_DIR`、发布日志 `RELEASE_HISTORY`、健康检测超时 `HEALTH_TIMEOUT`
2. `SERVICE_BUILD_DIR`：服务名 → 源码目录，映射可执行构建的服务
3. `SERVICE_DOCKERFILE`：服务名 → Dockerfile 文件名（可选，不构建可省略）
4. `HEALTH_CHECK_LIST`：服务名 → actuator 健康端口（容器内端口，未配置则跳过探活）
5. `ROLLBACK_SERVICE_LIST`：走平滑滚动发布的服务白名单（其余服务走停机发布；**平滑服务必须配置健康检测端口**（见第 4 点），否则脚本会拒绝执行；**平滑服务不能绑定宿主端口**，否则副本扩容会端口冲突）

## 五、使用示例

| 命令 | 说明 |
|---|---|
| `deploy.sh 1 your-service1` | 构建镜像 + 平滑发布 |
| `deploy.sh 0 your-service1` | 跳过构建，复用本地 latest 镜像直接发布 |
| `deploy.sh rollback your-service1` | 自动回滚到上一有效版本 |
| `deploy.sh rollback your-service1 20260818160000` | 回滚到指定 tag |

不传参数时默认执行 `deploy.sh 1 your-service1`。

## 六、平滑发布原理

1. 预检：校验 compose 文件合法性，采集当前副本数、上一版本 tag、旧容器 ID 列表
2. 无运行副本：直接 `docker compose up -d`，不走平滑流程
3. 有运行副本时逐轮滚动（共副本数轮）：
   - 生成临时 override 文件指定新镜像 tag（不改动原 compose 文件）
   - `docker compose create --scale N+1 --no-recreate` 只创建新容器，旧容器不动
   - `start` 启动后通过容器内网 IP 探测 `/actuator/health`，超时上限 `HEALTH_TIMEOUT`
   - 健康通过 → 停止并删除一个旧容器 → 进入下一轮
4. 全部轮次完成：恢复原 compose 文件、写入发布历史

## 七、失败自动回滚

发布过程中任意环节失败（新容器起不来、探活超时等）：

1. 清理所有新容器（无论 running 还是 Created，健康失败即发布未成功）
2. 恢复 compose 文件为 latest
3. 按剩余新旧容器数量分支处理：
   - 新容器 0 个：旧容器全在，直接完成回退
   - 新 > 旧：镜像本身没问题，保留新容器，提示人工清理剩余旧容器
   - 新 ≤ 旧且 > 0：用旧镜像创建替代容器 → 探活 → 通过后删掉全部新容器（零停机回退）；替代容器也失败则打印诊断信息，提示人工介入

## 八、并发锁机制

- 按服务粒度加锁（锁文件 `/tmp/deploy-<服务名>.lock`），同一服务不允许并发发布/回滚，不同服务互不阻塞
- 锁覆盖构建、发布、回滚全流程，随进程退出自动释放，无需手动解锁
- 拿锁失败时打印持有者信息（PID / 服务 / 开始时间）并终止本次执行
- 如需强制结束占用中的发布：`kill -9 <PID>`，然后重试
- **禁止删除锁文件来"解锁"**：flock 锁绑定在进程的文件描述符上，rm 锁文件会导致旧进程继续持有、新进程重新建锁，形成双跑，比不加锁更危险

## 九、边界与注意事项

- 平滑发布依赖 `create --scale + 1 --no-recreate` 的增量创建行为，属于 compose 稳定选项，但建议保持 docker compose 为较新版本
- 滚动期间新旧副本短暂并存：前置网关/代理需能将流量分发到所有副本，且应用需兼容新旧版本并存（如数据库变更要向后兼容）
- 探活依赖 `/actuator/health` 返回 200，需确认 SecurityConfig 已对 actuator 路径免认证放行
- 单机部署方案；多机环境可在每台服务器各放一份脚本分别执行
- 发布历史文件被误删后，自动回滚将降级为"无历史可回滚"，需手动指定 tag

## 十、常见问题

| 问题 | 处理 |
|---|---|
| 提示"已有发布任务在运行" | 查看占用信息中的 PID，确认后 `kill -9 <PID>` 再重试 |
| 锁文件无占用信息 | 多为残留空文件，确认无发布进程后可直接重试（锁不受影响） |
| 回滚提示"无法获取上一个版本tag" | 发布历史为空或已删，改用 `rollback <服务> <tag>` 指定 tag |
| 健康检测一直不通过 | `docker compose logs -f <服务>` 查看应用日志；确认端口与 actuator 路径配置正确 |
